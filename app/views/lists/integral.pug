//- 积分记录列表;
each item, index in list
    li
        dl
            dt #{item.des}
            dd
                //-p 订单号：#{item.related_no}
                time #{item.create_time}
        if item.math_operator
            sup +#{item.point_num}
        else
            sub -#{item.point_num}