include ../../includes/mixin.pug
-var inlet = 1, extra = '&shopId=' + request.shopId;
if tab && tab.length
    .goods
        .tab.js-goods-tab
            ul
                li(class="active", data-id=-1)
                    a(href='javascript:;') 全部
                each t, i in tab
                    li(data-id=t.id)
                        a(href='javascript:;') #{t.name}
        ul.u-goods-list.pagination.clearfix
            +goodsList(goodsList)
else
    .u-null-all
        .u-null
            dl
                dt.n-01
                dd
                    p 暂时没有商品~
